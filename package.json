{
  "name": "phill-job-application-copilot",
  "private": true,
  "version": "1.0.0-stage1",
  "description": "Opportunity Command Center — Stage 1 intelligence and review dashboard",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "install:frontend": "npm install --prefix frontend",
    "pages:deploy": "npm run build && wrangler pages deploy frontend/dist --project-name=phill-job-application-copilot",
    "cf:deploy": "npm run build && wrangler deploy -c wrangler.worker.toml"
  },
  "workspaces": [
    "frontend"
  ]
}
