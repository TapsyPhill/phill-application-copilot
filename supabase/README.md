# Supabase

Apply `migrations/20260525100000_stage1_core_schema.sql` in the Supabase SQL editor or via CLI.

Enables `vector` extension for embeddings. RLS policies assume `authenticated` single-user access; backend scripts use the service role key.
