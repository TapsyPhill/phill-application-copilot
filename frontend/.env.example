# Frontend dev loads env from repo ROOT — copy / use ../.env
# Required in root .env (not here):
#   VITE_SUPABASE_URL=
#   VITE_SUPABASE_PUBLISHABLE_KEY=
#   VITE_PROJECT_DOMAIN=phill-job-application-copilot.pages.dev
