# Backend

Python modules for scraping, ingestion, deduplication, AI analysis, RAG, and opportunity scoring.

Run scripts from repository root with `PYTHONPATH=.` or `python scripts/...` (scripts add root to path).

See [docs/stage-1-architecture.md](../docs/stage-1-architecture.md).
